version: 1
frontend:
  phases:
    build:
      commands:
        - echo "No build steps required - static HTML/CSS/JS only"
  artifacts:
    baseDirectory: src
    files:
      - '**/*'
  cache:
    paths: [] 